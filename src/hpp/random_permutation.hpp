#ifndef RANDOM_PERMUTATION_HPP
#define RANDOM_PERMUTATION_HPP

#include <vector>

using namespace std;

// Generate a random permutation of the numbers {0, 1, ..., N-1}
vector<int> generateRandomPermutation(int N);

#endif // RANDOM_PERMUTATION_HPP